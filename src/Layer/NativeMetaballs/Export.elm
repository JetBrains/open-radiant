module Layer.NativeMetaballs.Export exposing (..)

foo = 42

-- use Fluid layer export/import


            -- layerModelDecoder M.Fluid product |>
            --     D.map (\layerModel ->
            --         case layerModel of
            --             M.FluidModel fluidModel -> M.NativeMetaballsModel fluidModel
            --             _ -> layerModel
            --         )
